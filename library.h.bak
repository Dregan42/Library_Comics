#ifndef __LIBRARY__
#define __LIBRARY__

#include "comics.h"

typedef struct library Library;

typedef struct library
	{
		int nbBooks;
		int nbBookMax;
		Comics* comics;
	} Library;

Library* create_library();
Library* add_comics_to_library();
Library* remove_comics_to_library(int comics_id);
void show_library();
void show_comics(int comics_id);


#endif
